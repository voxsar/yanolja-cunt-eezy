import React from 'react';

const RoomView: React.FC = () => {
  return (
    <div className="room-view-container">
      <h1>Room View (Card View)</h1>
      <p>Room cards with current status will be displayed here.</p>
    </div>
  );
};

export default RoomView;
